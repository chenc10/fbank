These code is for detecting whether there are replicated softs in the database,
if yes then the replicated softs are deleted.
Be noted that deleting occurs only when two softs within one class (benign/malware) is same.
